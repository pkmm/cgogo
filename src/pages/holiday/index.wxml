<view class='page'>
    <view class='page__bd'>
        <view class="section section_gap" wx:for="{{images}}" wx:key="*this">
            <!-- <view class="section__title">{{item.text}}</view> -->
            <view class="section__ctn">
                <image style="width: 100%;" mode="{{item.mode}}" src="{{item.url}}"></image>
            </view>
        </view>
    </view>
</view>